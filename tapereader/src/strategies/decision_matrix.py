class DecisionMatrix:
    def __init__(self, config, behavior_manager, regime_classifier, confluence_analyzer, signal_tracker):
        self.config = config
    async def evaluate(self): return []
